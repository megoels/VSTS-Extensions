{
  "name": "diableagent",
  "version": "1.0.0",
  "description": "Disable the running agent on build failure. This might be used for debugging issues on host if build fail",
  "main": "disableAgent.js",
  "dependencies": {
    "path": "^0.12.7",
    "vso-node-api": "^6.2.4-preview",
    "vsts-task-lib": "^2.0.5"
  },
  "devDependencies": {
    "@types/node": "^7.0.43",
    "@types/q": "^1.0.5"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
